{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run tsc:watch --parallel",
    "test": "jest",
    "publish": "lerna run tsc && lerna publish"
  },
  "devDependencies": {
    "@types/jest": "^23.3.5",
    "@types/node": "^10.12.0",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "ts-jest": "^23.10.4"
  }
}
