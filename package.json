{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run tsc:watch --parallel",
    "test": "jest",
    "test:watch": "jest --watch",
    "publish": "lerna run tsc && npm test && lerna publish"
  },
  "devDependencies": {
    "@types/jest": "^23.3.5",
    "@types/node": "^10.12.0",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "rimraf": "^2.6.2",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "ts-jest": "^23.10.4"
  }
}