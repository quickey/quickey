{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch --coverage",
    "release": "lerna run tsc && lerna run build && npm test && lerna publish",
    "tsc:watch": "lerna run tsc:watch --parallel",
    "clean": "lerna exec rimraf lib/"
  },
  "devDependencies": {
    "@types/jest": "^23.3.5",
    "@types/node": "^10.12.0",
    "clean-webpack-plugin": "^0.1.19",
    "jest": "^23.6.0",
    "lerna": "^3.4.3",
    "rimraf": "^2.6.2",
    "ts-loader": "^5.2.2",
    "typescript": "^3.1.3",
    "webpack": "^4.21.0",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "ts-jest": "^23.10.4"
  }
}
